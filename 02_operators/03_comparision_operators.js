/**
 * Compare two values and return a Boolean value (true or false):
    == (Equal to): Compares values (with type coercion).
    === (Strict equal to): Compares both value and type (recommended).
    != (Not equal to): Checks for inequality (with type coercion).
    !== (Strict not equal to): Checks if values or types are different.
    > (Greater than), < (Less than), >= (Greater than or equal to), <= (Less than or equal to).
 */