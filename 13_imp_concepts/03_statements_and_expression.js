/**
 * At a Glance
    Expression: - A piece of code that evaluates to a single value (e.g., 2 + 2).
                - Expression produce a value
                Examples:
                    5 (Literal value)
                    x + 10 (Arithmetic)
                    Math.random() (Function call)
                    isTrue ? 'Yes' : 'No' (Ternary operator)

    Statement:  - A standalone unit of code that instructs the computer to do something (e.g., let x = 5;). 
                - Statement	Perform an action
                Examples:
                    let x = 10; (Variable declaration)
                    if (x > 5) { ... } (Conditional)
                    for (let i = 0; i < 10; i++) { ... } (Loop)
                    return true; (Jump statement

 */